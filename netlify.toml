[build]
  command = "npm --prefix backend ci && npm --prefix backend run prepare:netlify && npm --prefix backend run build"
  publish = "netlify-site"
  functions = "backend/netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pg"]