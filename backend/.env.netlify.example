# Add these in Netlify: Project configuration -> Environment variables.
# Never upload this file or backend/.env to the published site.
DATABASE_URL=your-neon-pooled-connection-string
JWT_ACCESS_SECRET=at-least-32-random-characters
JWT_REFRESH_SECRET=at-least-32-random-characters
ALLOWED_ORIGINS=https://effulgent-baklava-550cd4.netlify.app
NODE_ENV=production